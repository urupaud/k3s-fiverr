# Apply kubernetes configs to k3s cluster.

Once k3s cluster has been created and you have access to the cluster via kubectl, please execute this tf code in following order.

`terrraform init`

`terraform apply`

