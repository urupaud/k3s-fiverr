# Default values for reloadebayspecifications.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicas: 1

oncloud:
    enabled: false

hostname: dev-flaskapi.emi.pe

Image: flask_api:local
